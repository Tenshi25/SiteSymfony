{% extends 'base.html.twig' %}
{% block title %}Menu Admin{% endblock %}
{% block body %}
    <div class="container" style="margin-top :10px">
        <div class="row">
        </div>
        <div class="row">
            <div class="col-xs-2 col-sm-2"></div>
            <div class="col-xs-8 col-sm-8">
                <div class="panel panel-primary">
                    <div class="panel-heading text-center" style="background-image:linear-gradient(to bottom,#b92c28 0,#b92c28 100%);"><h2>Menu des Pages</h2></div>
                    <div class="panel-body">
                        <div class="form-group">
                            <a class="btn btn-danger btn-lg btn-block" type="button" href="{{ path('admin_page_index') }}" >Gerer les pages</a>
                        </div>
                        <div class="form-group">
                            <a class="btn btn-danger btn-lg btn-block" type="button" href="{{ path('admin_themepage_index') }}" >Gerer les theme des pages</a>
                        </div>
                        <div class="form-group">
                            <a class="btn btn-danger btn-lg btn-block" type="button" href="{{ path('admin_photo_index') }}" >Gerer les photo</a>
                        </div>
                        <div class="form-group">
                            <a class="btn btn-danger btn-lg btn-block" type="button" href="{{ path('menuAdmin') }}" >Retour</a>
                        </div>
                    </div>
                    <div class="col-xs-2 col-sm-2"></div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}