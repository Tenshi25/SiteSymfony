{% extends 'base.html.twig' %}

{% block body %}
    <h1>Typegroupe</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeGroupe.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ typeGroupe.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typegroupe_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typegroupe_edit', { 'id': typeGroupe.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
            {{ form_widget(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
