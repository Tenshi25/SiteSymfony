{% extends 'base.html.twig' %}

{% block body %}
    <h1>Statut</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ statut.nom }}</td>
            </tr>
            <tr>
                <th>Ordre</th>
                <td>{{ statut.ordre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_statut_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_statut_edit', { 'id': statut.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
